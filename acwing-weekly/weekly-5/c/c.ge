#include <bits/stdc++.h>
#include "array.h"
#include "tree.h"
using namespace std;
using ll = long long;
#define all(x) (x).begin(), (x).end()

void generate() {
  int N = 2000;
  puts(N);
  for (int i = 0; i < N; i++) {
    int x = rnd.next(1000, (int) 1e6);
    int y = rnd.next(1000, (int) 1e6);
    puts(x, y);
  }
  auto c = Array::random(N, 1000, (int) 1e9);
  auto k = Array::random(N, 1000, (int) 1e9);
  puts(c);
  puts(k);
}

int main() {
  generate();
  return 0;
}
