/* Name        : a
 * Creat       : Fri Jul 12 22:17:16 CST 2019
 * Accept      : Fri Jul 12 22:17:54 CST 2019
 * Author      : landcold7
 * Action speaks louder more than words
 */
#include <bits/stdc++.h>
using namespace std;

#define x first
#define y second
#define sz(x) ((int)(x).size())
#ifndef _has_trace
#define trace(...)
#endif
typedef long long ll;

void solve() {
  int x, a;
  cin >> x >> a;
  if (x < a) {
    cout << 0 << "\n";
  }
  else {
    cout << 10 << "\n";
  }
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  solve();
  return 0;
}
