#include <bits/stdc++.h>
#include "array.h"
#include "tree.h"
using namespace std;
using ll = long long;
#define all(x) (x).begin(), (x).end()

void generate() {
  puts(1);
  int N = 1000;
  int K = 1e6;
  puts(N, K);
  auto B = Array::random(N, N, N);
  auto C = Array::random(N, K, K);
  puts(B);
  puts(C);
}

int main() {
  generate();
  return 0;
}
