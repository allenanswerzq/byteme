#include <bits/stdc++.h>
#include "array.h"
#include "tree.h"
using namespace std;
using ll = long long;
#define all(x) (x).begin(), (x).end()

void generate() {
  int N = 35000;
  puts(1);
  puts(N);
  auto a = Array::randomUnique(N, 1, N);
  puts(a);
}

int main() {
  generate();
  return 0;
}
