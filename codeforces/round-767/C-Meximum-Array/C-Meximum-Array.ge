#include <bits/stdc++.h>
#include "array.h"
#include "tree.h"
using namespace std;
using ll = long long;
#define all(x) (x).begin(), (x).end()

void generate() {
  puts(1);
  int N = 200000;
  puts(N);
  puts(Array::random(N, 0, 3));
}

int main() {
  generate();
  return 0;
}
