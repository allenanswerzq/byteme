#include <bits/stdc++.h>
#include "array.h"
#include "tree.h"
using namespace std;
using ll = long long;
#define all(x) (x).begin(), (x).end()

void generate() {
  puts(1);
  int n = rnd.next(1, 200000);
  puts(n);
  puts(Array::random(n, 1, n));
}

int main() {
  generate();
  return 0;
}
